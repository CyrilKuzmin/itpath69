{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${cwd}/cmd/",
            "cwd": "${cwd}",
            "env": {
                "TEST": "cyril"
            }
        }
    ]
}